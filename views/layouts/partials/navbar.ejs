<link rel="stylesheet" href="/css/navbar.css">
<nav class="navbar navbar-expand-lg navbar-light bg-light sticky-top navbar-custom">

  <div class="container">
    <a class="navbar-brand" href="/">
      <img src="/images/logo/logo.png" alt="PlayCourt" height="45">
    </a>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
      aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav mx-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link <%= (url === '/' ? 'active' : '') %>" href="/">Home</a>
        </li>

        <% if (currentUser.userType !=1) { %>
          <li class="nav-item">
            <a class="nav-link <%= (url === '/mylevel' ? 'active' : '') %>" href="/mylevel">My Level</a>
          </li>
        <% } %>

        <% if (currentUser.userType ==1) { %>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="masterDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              Master
            </a>
            <ul class="dropdown-menu">
              <li><a class="dropdown-item" href="/levels">Levels</a></li>
              <li><a class="dropdown-item" href="/sponsor">Sponsor</a></li>
            </ul>
          </li>
        <% } %>

        <li class="nav-item">
          <a class="nav-link <%= (url === '/players' ? 'active' : '') %>" href="/players">Players</a>
        </li>

        <% if (currentUser.userType ==1) { %>
          <li class="nav-item">
            <a class="nav-link <%= (url === '/levelup' ? 'active' : '') %>" href="/levelup">Levels</a>
          </li>
        <% } %>

        <li class="nav-item">
          <a class="nav-link <%= (url === '/tournaments' ? 'active' : '') %>" href="/tournaments">Tournament</a>
        </li>
      </ul>

      <ul class="navbar-nav">
        <% if (currentUser) {%>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              <i class="fa-solid fa-user"></i>
              <%= currentUser.username %>
            </a>
            <ul class="dropdown-menu dropdown-menu-end">
              <li class="dropdown-item disabled">
                <span class="text-sm" style="color: black;"><%= currentUser.fullname %></span>
                <br>
                <span class="text-sm" style="font-size: 0.875rem; color: #6c757d;"><%= currentUser.email %></span>
              </li>
              <li><a class="dropdown-item" href="#"><i class="fa-solid fa-pen-to-square"></i> My Profile</a></li>
              <li>
                <a class="dropdown-item" href="/logout" onclick="event.preventDefault(); document.getElementById('logout-form').submit();">
                  <i class="fa-solid fa-right-from-bracket"></i> Logout
                </a>
                <form action="/logout" method="POST" id="logout-form" style="display:none;"></form>
              </li>
            </ul>
          </li>
        <% } %>
      </ul>

    </div>
  </div>
</nav>
